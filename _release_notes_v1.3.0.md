# Steam 好友收割机 v1.3.0 · 好友代码支持 + 自动驾驶

## 🆕 好友代码（小黑盒评论区常见格式）

- 小黑盒评论区里大家贴的多数不是 17 位 Steam64，而是 **Steam 好友代码**（客户端「添加好友」页显示的 5~10 位短数字）——现在能直接识别并自动换算成 Steam64 再入队
- 换算规则：`Steam64 = 好友代码 + 76561197960265728`，并校验落在真实区间（76561197960265728 ~ 76561202255233023），非法数字自动丢弃
- 所有入口统一支持：小黑盒评论区收割、弹窗「📂 导入」粘贴文本、手动入队——后台统一归一化
- **顺带修复一个隐藏 bug**：老代码所有校验只认 `7656119` 开头的 Steam64，会漏掉 `7656120` 开头的新账号区间 ID（好友代码换算大于 4294967295-3645823552 的都会落在这个区间）；现已放宽为完整真实区间，17 位正则、黑/白名单校验同步修正

## 🆕 自动驾驶（浏览器开着就自动加）

- 设置「调度」区新增 **自动驾驶** 开关（默认开）
- 打开后：后台每分钟检查一次，**队列非空且没在跑就自动开跑一批**，完全不用点「开始」
- 速率沿用你设置的间隔（默认 8~15 秒随机）+ 自适应拉长 + 黄金时段 + 日配额，达到配额或时段外自动停
- 关闭浏览器即停；重新打开后自动继续清队列
- 自动模式下「未登录 / 配额满」不再弹桌面通知（每分钟检查会刷屏），跑完一批仍正常通知

## 文件变更

- `content/common.js` — 好友代码正则 + `friendCodeToSteam64` 换算（BigInt 区间校验）+ Steam64 正则放宽到 7656119/7656120
- `background/service-worker.js` — `normalizeSteamId` 统一归一化（入队/黑名单/白名单全走它）；`autoRun` 设置 + `sfp_autorun` 闹钟 + `maybeAutoRun` 调度；`runBatch({auto})` 静默错误通知
- `popup/popup.html` / `popup.js` — 设置区「自动驾驶」开关
- 新增 `_smoke_friendcode.js` 冒烟：真实执行 common.js 提取逻辑 + SW 归一化函数（6+9 组用例，含 7656120 区间、32 位上限边界）

## 升级

浏览器内一键更新：弹窗顶部橙色横幅（或设置页）点「🚀 一键更新」→ 等进度走完 → 扩展页「重新加载」↻。
