---
description: UI 界面数值输入强制规则 —— 时间/百分比/次数类参数必须用原生数值控件；输入框内只放半角数字；单位文字放输入框外部独立标签；禁止占位符/罗马数字/全角符号/静态模板文本。任何涉及 UI 表单、设置页、对话框、弹窗输入框的改动都必须遵守。
alwaysApply: true
enabled: true
updatedAt: 2026-09-15T00:00:00.000Z
provider:
---

# UI 数值输入强制规则

适用：所有含图形界面（UI）的项目与代码，包括但不限于 PySide6/Qt、WinForms/WPF、HTML/JS/CSS、Electron、C# IMGUI。
凡是新增或修改「时间、百分比、次数、数量、间隔、阈值、字号、延时」等数值类输入，**必须**满足以下 5 条。

1. **必须使用原生数值控件**
   - 时间 / 百分比 / 次数类参数一律用原生数值控件承载：
     - Qt/PySide6：`QSpinBox` / `QDoubleSpinBox`
     - WinForms/WPF：`NumericUpDown` / `Slider`
     - Web：`<input type="number">`（或 `type="range"`）
     - C# IMGUI：`GUILayout.IntField` / `FloatField` + 合法值钳制，或滑条
   - **禁止**用普通文本框（`QLineEdit` / `TextBox` / `<input type="text">`）承载数值。

2. **输入框内部只存放半角阿拉伯数字**
   - 框内内容只允许半角 `0123456789`，可含半角小数点与半角负号（如 `0.5`、`-1`）。
   - 禁止空格、逗号、中文、全角数字、单位后缀、说明文字出现在框内。

3. **单位文字放输入框外部，作为独立标签**
   - `ms`、`s`、`秒`、`分钟`、`%`、`次`、`px` 等单位必须放在输入框**外部**（独立 `QLabel` / `NumericUpDown` 旁的 `Label` / HTML 相邻 `<span>` / 后缀标签），
     **禁止**用 `setSuffix` / `setPrefix` 之外的任意形式把单位塞进输入框视觉内部——原则上不让单位出现在 `value` 里。
   - Qt 中如需紧贴输入框显示单位，用 `QSpinBox.setSuffix(" ms")`（后缀标签，不属于输入内容）或独立 Label；**不得**把单位写进框内文本。

4. **禁止占位符 / 罗马数字 / 全角符号**
   - 禁止使用罗马数字 `Ⅱ`、全角圆圈 `○`、`●`、`◆`、`√`、`※` 等特殊符号作为输入框占位文本。
   - 默认值直接写**有效数字**，例如 `100`、`10`、`0`，不要写 `--`、`○`、`默认`、`请输入`。

5. **禁止静态模板文本直接填充进输入框 value**
   - 禁止 `value="100 ms"`、`text="10%"`、`setText("间隔：200")` 这类把模板/说明文本写进输入框的做法。
   - 初始值必须是纯数字；说明文字放外部标签或 `label`/`title`。

## 自检清单（改完 UI 后逐条核对）

- [ ] 所有时间/百分比/次数输入控件是 `SpinBox`/`NumericUpDown`/`type=number`，不是文本框。
- [ ] 逐一打开每个输入框，框内字符只可能是 `0-9 . -`。
- [ ] 每个输入框的单位要么是控件原生 `suffix`，要么是紧邻的独立标签；框内无单位。
- [ ] 默认值是有效数字（`100`/`10`/`0`），不是符号或占位文案。
- [ ] 没有把说明文案 / 模板字符串塞进输入框初始值。
- [ ] 改动后功能回归正常（保存/读取配置、边界值钳制、空值回退默认）。

## 反例 → 正例

| 反例（禁止） | 正例（必须） |
|---|---|
| `QLineEdit("200 ms")` | `QSpinBox` + `setSuffix(" ms")` + 独立 Label |
| `<input type="text" value="10%">` | `<input type="number" value="10"> <span>%</span>` |
| `Edit` 里写 `○`、`Ⅱ` 当占位 | `Edit.Text = "0"`（有效数字默认值） |
| `setText("间隔：200")` | 数值 `200` 进 SpinBox，「间隔」进外部 Label |
| `TextBox` 让用户手打「3次」 | `NumericUpDown` = 3 + 外部 Label「次」 |
